<li>
  <%= gravatar_for user, size: Settings.users.avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
     <%= link_to t("user.delete"), user, method: :delete, data: {confirm: t("warning.delete")} %>
  <% end %>
</li>
